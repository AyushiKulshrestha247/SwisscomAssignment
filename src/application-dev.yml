spring:
  data:
    mongodb:
      uri: mongodb+srv://ayushikulshrestha247:gRm3Q2hvilPUeUja@ayushi.gl0fofk.mongodb.net/your_database_name?retryWrites=true&w=majority
      database: Assignment
  cache:
    type: simple

  logging:
    level:
      root: DEBUG
    file:
      name: logs/application.log  # All logs will be written here
    pattern:
      console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
      file: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    spring:
      profiles:
        active: dev


